/**
 * Clase IRepoDominio
 * Esta clase representa una interfaz para la gestión del repositorio de dominio en la aplicación.
 * Define un método gestionarRepoDominio que debe ser implementado por las clases que extiendan esta interfaz.
 */
class IRepoDominio {
    /**
     * Método gestionarRepoDominio
     * Método que debe ser implementado por las clases que extiendan esta interfaz.
     * (Actualmente no implementado)
     */
    gestionarRepoDominio() {}
}

module.exports = IRepoDominio;