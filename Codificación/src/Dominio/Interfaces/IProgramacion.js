/**
 * Clase IProgramacion
 * Esta clase representa una interfaz para la gestión de la programación en la aplicación.
 * Define un método mostrarProgramacion que debe ser implementado por las clases que extiendan esta interfaz.
 */
class IProgramacion {
    /**
     * Método mostrarProgramacion
     * Método que debe ser implementado por las clases que extiendan esta interfaz.
     * (Actualmente no implementado)
     */
    mostrarProgramacion() {}
}

module.exports = IProgramacion;