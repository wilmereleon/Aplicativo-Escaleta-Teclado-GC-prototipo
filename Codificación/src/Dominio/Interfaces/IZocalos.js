/**
 * Clase IZocalos
 * Esta clase representa una interfaz para la gestión de zócalos en la aplicación.
 * Define un método mostrarZocalos que debe ser implementado por las clases que extiendan esta interfaz.
 */
class IZocalos {
    /**
     * Método mostrarZocalos
     * Método que debe ser implementado por las clases que extiendan esta interfaz.
     * (Actualmente no implementado)
     */
    mostrarZocalos() {}
}

module.exports = IZocalos;