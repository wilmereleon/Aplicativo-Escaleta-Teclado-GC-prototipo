/**
 * Clase IServicios
 * Esta clase representa una interfaz para la gestión de servicios en la aplicación.
 * Define un método gestionarServicios que debe ser implementado por las clases que extiendan esta interfaz.
 */
class IServicios {
    /**
     * Método gestionarServicios
     * Método que debe ser implementado por las clases que extiendan esta interfaz.
     * (Actualmente no implementado)
     */
    gestionarServicios() {}
}

module.exports = IServicios;