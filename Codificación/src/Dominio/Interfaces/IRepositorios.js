/**
 * Clase IRepositorios
 * Esta clase representa una interfaz para la gestión de repositorios en la aplicación.
 * Define un método gestionarRepositorios que debe ser implementado por las clases que extiendan esta interfaz.
 */
class IRepositorios {
    /**
     * Método gestionarRepositorios
     * Método que debe ser implementado por las clases que extiendan esta interfaz.
     * (Actualmente no implementado)
     */
    gestionarRepositorios() {}
}

module.exports = IRepositorios;