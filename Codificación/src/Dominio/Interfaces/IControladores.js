/**
 * Clase IControladores
 * Esta clase representa una interfaz para los controladores en la aplicación.
 * Define un método gestionarAplicacion que debe ser implementado por las clases que extiendan esta interfaz.
 */
class IControladores {
    /**
     * Método gestionarAplicacion
     * Método que debe ser implementado por las clases que extiendan esta interfaz.
     * (Actualmente no implementado)
     */
    gestionarAplicacion() {}
}

module.exports = IControladores;