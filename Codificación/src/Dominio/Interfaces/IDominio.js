/**
 * Clase IDominio
 * Esta clase representa una interfaz para los dominios en la aplicación.
 * Define un método gestionarDominio que debe ser implementado por las clases que extiendan esta interfaz.
 */
class IDominio {
    /**
     * Método gestionarDominio
     * Método que debe ser implementado por las clases que extiendan esta interfaz.
     * (Actualmente no implementado)
     */
    gestionarDominio() {}
}

module.exports = IDominio;