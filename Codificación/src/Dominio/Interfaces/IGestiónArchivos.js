/**
 * Clase IGestiónArchivos
 * Esta clase representa una interfaz para la gestión de archivos en la aplicación.
 * Define un método gestionarArchivos que debe ser implementado por las clases que extiendan esta interfaz.
 */
class IGestiónArchivos {
    /**
     * Método gestionarArchivos
     * Método que debe ser implementado por las clases que extiendan esta interfaz.
     * (Actualmente no implementado)
     */
    gestionarArchivos() {}
}

module.exports = IGestiónArchivos;