const Fila = require('./Fila');

/**
 * Clase FilaPlaca
 * Esta clase representa una fila de tipo 'Placa' en la escaleta.
 * Extiende la clase Fila.
 */
class FilaPlaca extends Fila {
  /**
   * Método especificoPlaca
   * Método específico para la fila de placa.
   * (Actualmente no implementado)
   */
  especificoPlaca() {}
}

module.exports = FilaPlaca;