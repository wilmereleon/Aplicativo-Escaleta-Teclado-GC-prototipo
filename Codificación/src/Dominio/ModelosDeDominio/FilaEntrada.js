const Fila = require('./Fila');

/**
 * Clase FilaEntrada
 * Esta clase representa una fila de tipo 'Entrada' en la escaleta.
 * Extiende la clase Fila.
 */
class FilaEntrada extends Fila {
  /**
   * Método especificoEntrada
   * Método específico para la fila de entrada.
   * (Actualmente no implementado)
   */
  especificoEntrada() {}
}

module.exports = FilaEntrada;