const Fila = require('./Fila');

/**
 * Clase FilaVTRFull
 * Esta clase representa una fila de tipo 'VTR Full' en la escaleta.
 * Extiende la clase Fila.
 */
class FilaVTRFull extends Fila {
  /**
   * Método especificoVTRFull
   * Método específico para la fila de VTR Full.
   * (Actualmente no implementado)
   */
  especificoVTRFull() {}
}

module.exports = FilaVTRFull;