const Fila = require('./Fila');

/**
 * Clase FilaVTRNota
 * Esta clase representa una fila de tipo 'VTR Nota' en la escaleta.
 * Extiende la clase Fila.
 */
class FilaVTRNota extends Fila {
  /**
   * Método especificoVTRNota
   * Método específico para la fila de VTR Nota.
   * (Actualmente no implementado)
   */
  especificoVTRNota() {}
}

module.exports = FilaVTRNota;